# Paths
data_folder: "data"
db_file: "database.db"
raw_file: "articles.xml.bz2"
raw_metadata_file: "download_metadata.json"
prompts_file: "prompts.yaml"

# Model names
model_hf: "deepseek-ai/DeepSeek-V3"
model_openrouter: "deepseek/deepseek-chat"

# Number of tokens per chunk (for processing long articles)
max_tokens: 7000

# Wikipedia domain
domain: "simple" # https://simple.wikipedia.org/
domain_download: "simplewiki" # https://dumps.wikimedia.org/simplewiki/
